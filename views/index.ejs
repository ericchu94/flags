<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Flags</title>
  </head>
  <body>
    <div>
      <h1>Flags</h1>
      <ul>
      <% for (const name in flags) { %>
        <li><a href="/flags/<%= name %>"><%= name %></a> <a href="/manage/<%= name %>">Manage</a></li>
      <% } %>
      </ul>
    </div>
  </body>
</html>
